name = "hacker-hardware"
main = "cloudflare/workers.js"
compatibility_date = "2024-01-01"
workers_dev = true

# Account and zone configuration
# Note: These can be set via environment variables or Cloudflare dashboard
# account_id = ""  # Set via CLOUDFLARE_ACCOUNT_ID environment variable
# zone_id = ""  # Set via CLOUDFLARE_ZONE_ID environment variable

# Routes configuration (optional - can be configured in Cloudflare dashboard)
# routes = [
#   { pattern = "hackerhardware.net/*", zone_name = "hackerhardware.net" }
# ]

[env.production]
name = "hackerhardware"
# route = "hackerhardware.net/*"

# KV Namespace binding for rate limiting
# Note: KV namespace can be configured via Cloudflare dashboard
# [[kv_namespaces]]
# binding = "RATE_LIMIT_KV"
# id = ""  # Set via Cloudflare dashboard or environment variable
